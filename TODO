* Complete History<T> class
* Study the bug of incorrect behaviour QFileDialog during quick file utilization 
* Study the bug with data transfer into RangeBox 
* Distinguish the parsing function and data sorting into the separate thread for keepeng the interface sensitivity
* Make spectra transfer into svg 
* Change the context menu invocation for HistogramWindow
* Change invocation of HistogramWindow (parent()->parent()->parent()) 
* change grey background with transtittance one 
* add figure: number - dt 
